---
description: Show engagement status: findings summary and active Herdr agent tabs. Usage: /status
agent: makima
---

Report the current state of the engagement:

1. List active Herdr tabs/agents (`herdr tab list`, `herdr agent list`).
2. Read the current engagement `notes.md` (look under
   `/workspace/engagements/`) and summarize: target, open ports, creds/hashes,
   flags captured so far, and next steps.
3. Present a concise status to the user, noting any sub-agent currently busy or
   blocked.
